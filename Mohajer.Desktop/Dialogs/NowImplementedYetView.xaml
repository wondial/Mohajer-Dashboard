<UserControl x:Class="Mohajer.Desktop.Dialogs.NowImplementedYetView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:local="clr-namespace:Mohajer.Desktop.Dialogs"
             mc:Ignorable="d">
    <Grid>
        <StackPanel Background="White">

            <Button
            HorizontalAlignment="Right"
            Margin="5"
            Command="{x:Static materialDesign:DialogHost.CloseDialogCommand}"
            Style="{StaticResource MaterialDesignFloatingActionMiniLightButton}"
	        ToolTip="MaterialDesignFlatButton">
                <materialDesign:PackIcon Foreground="Black" Kind="Close" Height="25" Width="24" />
            </Button>

            <TextBlock Text="این ویژگی هنوز تعبییه نشده است" FlowDirection="RightToLeft" Style="{StaticResource MaterialDesignTitleTextBlock}" Margin="15"/>
        </StackPanel>
    </Grid>
</UserControl>
